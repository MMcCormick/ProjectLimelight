<% if @talk.status == "active" %>
  <% content_for :sidebar_right do %>
    <%= render_cell(:core_object, :sidebar_right, current_user, @talk) %>
  <% end %>

  <div class="object-show talk">
    <div class="createdC">
      <%= time_ago_in_words(@talk.created_at, false) %> ago by
      <%= user_link @talk.user_snippet %>
    </div>

    <% if can? :destroy, @talk %>
      <div class="ac disable hide" data-m="PUT" data-url="<%= disable_talk_path @talk %>">X</div>
    <% end %>

    <div class="content">
      <%= markdown(parse_mentions(@talk.content, @talk)) %>
    </div>
    <div class="clear"></div>

  </div>
<% else %>
  <div class="empty">This talk has been removed</div>
<% end %>

<%= render_cell :comment, :thread, @talk.id, true %>
<% content_for :page_header do %>
  <h1><%= @title %></h1>
<% end %>

<% if current_user != @user %>
  <% content_for :sidebar_right do  %>
    <%#= render_cell(:user, :sidebar, @user, current_user, '') %>
  <% end %>
<% end %>

<% if !@following_users.empty? %>
  <ul class="alt-list">
    <li class="labels">
      <div>User</div>
      <div>Popularity</div>
      <div>Followers</div>
    </li>
    <%= render 'users/std_list', :users => @following_users %>
  </ul>
<% else %>
  <div class="empty">This user is not following any users!</div>
<% end %>
<%= f.fields_for AssetImage.new do |i| %>
  <%= i.hidden_field :remote_image_url, :class => 'remote_image_url', :autocomplete => 'off' %>
  <%= i.hidden_field :image_cache, :class => "image_upload_cache" %>
  <div class="contribute_picture_image_data"></div>
<% end %>
<div id="<%= f.object.class.name.underscore %>_pupB"></div>
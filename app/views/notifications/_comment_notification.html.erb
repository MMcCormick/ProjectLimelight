<div class="notification reply <%= is_first %> <%= 'unread' unless notification.read %>">
  <div class="chat-grey-icon icon"></div>
  <%= user_link(notification.triggered_by, nil, base) %>
  <%= action_text(notification, base) %>
</div>
<%# Temporary, for testing user callbacks %>
<%= form_for user, :html => {:class => "ajax"} do |f| %>
  <div class="errors"></div>

  <div class="static">
    <div class="name">Username:</div>
    <div class="value"><%= user.username %></div>
  </div>

  <div class="field">
    <%= f.label :first_name, "First Name", :class => "" %><br />
    <%= f.text_field :first_name, :class => "" %>
  </div>

  <div class="field">
    <%= f.label :last_name, "Last Name" %><br />
    <%= f.text_field :last_name %>
  </div>

  <div class="field">
    <%= f.label :bio, "Bio (150 chars max)", :class => "bottom" %><br />
    <%= f.text_area :bio, :class => "bottom" %>
  </div>

  <div class="actions">
    <%= f.submit 'Update Settings', :class => 'submit' %>
  </div>
<% end %>

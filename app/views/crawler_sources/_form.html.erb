<%= form_for(@crawler_source) do |f| %>
  <% if @crawler_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crawler_source.errors.count, "error") %> prohibited this crawler_source from being saved:</h2>

      <ul>
      <% @crawler_source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source_name %><br />
    <%= f.text_field :source_name %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h5>
  <% if @post.get('primary_source'): %>
    <span class="title title-with-source"><%= @post.get('title') %></span>
    <a target="_blank" href="<%= @post.get('primary_source').url %>">
      <span class="source"><%= @post.get('primary_source').name %></span>
    </a>
  <% else: %>
    <span class="title"><%= @post.get('title') %></span>
  <% end %>
</h5>

<% if @post.get('images'): %>
  <div class='img'>
    <img src='<%= @post.get('images').fit.large %>' alt='' />

    <% if @post.get('video'): %>
      <div class='ll-video-play'></div>
    <% end %>
    <div class='bg'></div>
  </div>
<% end %>

<% if @post.get('topic_mentions').length > 0: %>
  <div class="mentions">
    <% for mention, i in @post.get('topic_mentions'): %>
      <a class="tlink" data-id='<%= mention.get('id') %>' href="/<%= mention.get('slug') %>"><%= mention.get('name') %></a><%= "," if i + 1 < @post.get('topic_mentions').length %>
    <% end %>
  </div>
<% end %>

<div class='actions'>
  <span class='talk-form'>+Talk</span>
</div>

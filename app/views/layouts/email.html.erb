<%
 #TODO: This sucks.
 if Rails.env.development?
   base = 'http://localhost:3000'
 elsif Rails.env.staging?
   base = 'http://staging.projectlimelight.com'
 else
   base = 'http://www.projectlimelight.com'
 end
%>

<div align="center">
  <a href="<%= base %>">
    <%= image_tag base + image_path('logo_black.gif'), :style => 'max-width: 259px; max-height: 61px; margin-bottom: 15px;', :alt => "" %>
  </a>
</div>

<%= yield %>

<div>
  <a href="<%= base %>/users/marc" target="_blank">Marc</a> and <a href="<%= base %>/users/matt" target="_blank">Matt</a><br />
  -The Limelight Team
</div>

<br/>
<p style="font-size: 11px; color: #777; margin: 5px 0 0 0; cursor: default;">If you wish to change the frequency of these emails, please log in and click "Settings" on the left panel.</p>

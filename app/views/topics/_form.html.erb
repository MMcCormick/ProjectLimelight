<%= form_for @topic, :html => {:class => "ajax group"} do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Name", :class => "bottom" %><br />
    <%= f.text_field :name, :class => "bottom" %>
  </div>

  <div class="field">
    <%= f.label :summary, "Summary", :class => "bottom" %>
    <%= f.text_area :summary %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

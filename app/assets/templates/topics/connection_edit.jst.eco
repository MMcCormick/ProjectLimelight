<%= @connection.name %> <div class="delete">[delete]</div> <div class="primary <%= 'on' if @connection.uuid == @topic.get('primary_type_id') %>">[<%= if @connection.uuid == @topic.get('primary_type_id') then 'is primary' else 'make primary' %>]</div>